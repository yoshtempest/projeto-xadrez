function getQueenMoves(index, board) {
    if (target === "whiteQueen") {
    const lineMoves = getLineMoves(n, values);
    const diagonalMoves = getDiagonalMoves(n, values);
    scopes = lineMoves.concat(diagonalMoves);  // Combina os movimentos
    }
}